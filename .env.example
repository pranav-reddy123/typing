# Leave both unset to run entirely against IndexedDB + BroadcastChannel.
# Set both to switch every read, write and race onto Postgres + Supabase Realtime.
# Selection happens once, in src/lib/data/index.ts — no other file changes.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
